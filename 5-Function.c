#include <stdio.h>

int main() {
    // system("cls");

    // int a;
    // printf("Num: ");
    // scanf("%d", &a);
    // if(a==0) return 0;
    // printf("Num returned: %d\n", main());

    return 0;
}