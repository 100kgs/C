#include <stdio.h>

int main() {
    // system("cls");

    // int a;
    // printf("Num: ");
    // scanf("%d", &a);
    // if(a==0) return 0;
    // printf("Num returned: %d\n", main());

    int a = 1;
    printf(" %d %d %d", a, a++, ++a);
    return 0;
}